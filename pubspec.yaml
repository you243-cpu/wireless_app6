name: FertiLitef
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.1

environment:
  sdk: '>=3.9.0 <4.0.0'
  
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  flutter_cube:
    git:
      url: https://github.com/zesage/flutter_cube.git
      ref: master
  http: ^1.2.2
  fl_chart: ^0.68.0
  percent_indicator: ^4.2.3
  csv: ^6.0.0
  file_picker: ^8.0.5
  intl: ^0.19.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/simulated_soil_square.csv
    - assets/models/
